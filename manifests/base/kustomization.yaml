apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: grimoirelab-base

resources:
  - namespace
  - config
  - storage
  - databases
  - opensearch
  - apps
  - networking
#  - cronjobs

labels:
  - pairs:
      app.kubernetes.io/name: grimoirelab